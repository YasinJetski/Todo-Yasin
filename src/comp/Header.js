const Header = () => {

  return (
    <header className = "header">
      <h1>Todo App</h1>
      {/* <Button color = "green" text = "Add" onClick={onClick}/>*/} 
    </header>
  )
}

export default Header